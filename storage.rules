rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access on all files.
    // You should restrict this in a real production app.
    match /{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
